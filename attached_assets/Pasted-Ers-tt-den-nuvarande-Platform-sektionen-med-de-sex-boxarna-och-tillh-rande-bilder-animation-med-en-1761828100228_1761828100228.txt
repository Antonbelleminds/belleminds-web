Ersätt den nuvarande Platform-sektionen (med de sex boxarna och tillhörande bilder/animation) med en ny säkerhetssektion på samma plats.

🔧 Vad som ska tas bort:

Ta bort alla nuvarande element i Platform-delen (de sex boxarna, texterna och bilderna/animationen).

Inget gammalt innehåll eller layout-element ska ligga kvar i DOM:en.

🧱 Vad som ska läggas till:

Skapa en ny sektion med fyra boxar i samma stil, storlek och layout som de gamla sex hade.

Placera sektionen exakt där den gamla låg.

Håll samma spacing, marginaler och responsivt grid (2x2 på desktop, 1x4 på mobil).

🎨 Designkrav:

Samma visuella stil som befintliga boxar (rundade hörn, gradientbakgrund, lätt skugga).

Färger: bakgrund gradient from-gray-900 to-gray-800, text text-white.

Hover-effekt: ljusare gradient hover:from-gray-800 hover:to-gray-700.

Endast rubriken (fet text) syns som standard.

Vid hover: visa beskrivningen under rubriken med fade-in animation (opacity-0 → opacity-100, transition-opacity duration-300).

⚙️ Innehåll för boxarna:

1️⃣ GDPR-klar
– Vi designar alla tjänster för att uppfylla EU:s dataskyddsförordning. Användaren äger alltid sitt eget data.

2️⃣ EU-hostad drift
– All lagring och drift sker på servrar inom EU. Vi använder certifierade molntjänster med högsta säkerhetsnivå.

3️⃣ Säker kommunikation
– All datatrafik är krypterad från början till slut. OAuth 2.0 och moderna autentiseringsprotokoll skyddar varje anslutning.

4️⃣ Dataintegritet
– Vi garanterar att din information är korrekt, skyddad och oförändrad. Endast auktoriserade system och användare har åtkomst till dina resurser.

💡 Kodexempel (referens för funktionalitet, ej exakt layout):

<div className="grid grid-cols-1 sm:grid-cols-2 gap-6">
  {items.map((item) => (
    <div key={item.title}
      className="p-8 rounded-2xl bg-gradient-to-b from-gray-900 to-gray-800 text-white 
                 transition-all duration-300 hover:bg-gradient-to-b hover:from-gray-800 hover:to-gray-700">
      <h3 className="text-xl font-semibold mb-2">{item.title}</h3>
      <p className="text-gray-300 opacity-0 hover:opacity-100 transition-opacity duration-300">{item.desc}</p>
    </div>
  ))}
</div>


🧭 Syfte:
Ersätt hela Platform-sektionen med denna nya säkerhetsdel, behåll samma position i sidlayouten, men rensa bort tidigare bilder, animationer och texter.
Designen ska matcha resten av sidan och smälta in i det nuvarande mörka temat.